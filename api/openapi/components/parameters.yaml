OrgId:
  name: orgId
  in: path
  required: true
  schema:
    type: integer
    minimum: 1

UserId:
  name: userId
  in: path
  required: true
  schema:
    type: integer
    minimum: 1

ProjectId:
  name: projectId
  in: path
  required: true
  schema:
    type: integer
    minimum: 1

ResourceId:
  name: resourceId
  in: path
  required: true
  schema:
    type: integer
    minimum: 1

TranslationJobId:
  name: translationJobId
  in: path
  required: true
  schema:
    type: integer
    minimum: 1

BackendId:
  name: backendId
  in: path
  required: true
  schema:
    type: integer
    minimum: 1

EntryId:
  name: entryId
  in: path
  required: true
  schema:
    type: integer
    minimum: 1

Stage:
  name: stage
  in: path
  required: true
  schema:
    type: string
    enum: [translate, bootstrap]

TemplateId:
  name: templateId
  in: path
  required: true
  schema:
    type: integer
    description: 模板 ID。正数为用户模板，负数为内置模板。

PromptTemplateId:
  name: promptTemplateId
  in: path
  required: true
  schema:
    type: integer
    minimum: 1

TranslationProfileId:
  name: translationProfileId
  in: path
  required: true
  schema:
    type: integer
    minimum: 1
