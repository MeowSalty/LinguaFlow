OrgId:
  name: orgId
  in: path
  required: true
  schema:
    type: integer
    minimum: 1

UserId:
  name: userId
  in: path
  required: true
  schema:
    type: integer
    minimum: 1

ProjectId:
  name: projectId
  in: path
  required: true
  schema:
    type: integer
    minimum: 1

ResourceId:
  name: resourceId
  in: path
  required: true
  schema:
    type: integer
    minimum: 1

JobId:
  name: jobId
  in: path
  required: true
  schema:
    type: integer
    minimum: 1

BackendId:
  name: backendId
  in: path
  required: true
  schema:
    type: integer
    minimum: 1

EntryId:
  name: entryId
  in: path
  required: true
  schema:
    type: integer
    minimum: 1

TranslationPromptTemplateId:
  name: translationPromptTemplateId
  in: path
  required: true
  schema:
    type: integer

BootstrapPromptTemplateId:
  name: bootstrapPromptTemplateId
  in: path
  required: true
  schema:
    type: integer

ExecutionProfileId:
  name: executionProfileId
  in: path
  required: true
  schema:
    type: integer

ExecutionPlanTemplateId:
  name: executionPlanTemplateId
  in: path
  required: true
  schema:
    type: integer
    minimum: 1
