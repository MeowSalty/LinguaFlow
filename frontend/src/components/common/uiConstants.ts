/** Drawer / Modal 宽度档位（响应式：小屏留 32px 边距） */
export const DRAWER_WIDTH = {
  s: 'min(440px, calc(100vw - 32px))',
  m: 'min(560px, calc(100vw - 32px))',
  l: 'min(720px, calc(100vw - 32px))',
  xl: 'min(1120px, calc(100vw - 32px))',
} as const
