# 配置

::: warning 注意
此页面正在编写中，内容可能会有变动。
:::

## 环境变量

LinguaFlow 支持以下环境变量进行配置：

| 变量名 | 描述 | 默认值 |
|--------|------|--------|
| `LINGUAFLOW_PORT` | 服务监听端口 | `8080` |
| `LINGUAFLOW_DB_PATH` | 数据库文件路径 | `./data/linguaflow.db` |

## 配置文件

配置文件的详细说明将在后续版本中提供。
